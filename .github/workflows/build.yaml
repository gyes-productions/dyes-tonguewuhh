---
name: Placeholder Workflow

on:
  workflow_dispatch:

concurrency:
  group: build
  cancel-in-progress: true

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World!"

# TODO - Update this workflow to include Windows build using the local scripts.  
# Relocate scripts into a scripts folder.
# Update workflow to build on Mac OSX runner, doublecheck versions
# HISE building takes a long time - consider caching the HISE build or using a prebuilt version